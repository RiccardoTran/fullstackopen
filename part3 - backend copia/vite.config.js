// frontend/vite.config.js
import { defineConfig } from 'vite';

export default defineConfig({
  root: './',  // directory radice del progetto front-end
  build: {
    outDir: 'dist'  // directory di output
  }
});
